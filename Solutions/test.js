s = "this the a song of little joe, she was a girl i used to know, forever screaming all the day and night";

let unique = "";
// count no. of times it occurs
for (let element of s) {
    //compare against rest of characters
    for (var i=0; s.length-1; i++){
    //if (element != element[i] )
    if(unique=s.includes(element)===false)
    {
        
        
        return(s.indexOf("unique"))
    }else{return(-1);}
    }}
    