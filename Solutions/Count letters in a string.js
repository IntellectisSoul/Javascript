arr=[1,2,3,4,5,6]

var mp = new Map();
string = "this the a song of little joe, she was a girl i used to know, forever screaming all the day and night";
for (var element of string) {
    if (mp.has(letter)) {
        mp.set(letter, mp.get(letter)+1)
    } else (mp.set(letter, 1 ))
        
    }
console.log (mp.get("a"))  //type letter as input


//extract only unique characters
let result = mp.includes("world", 0);
console.log(result);


//Method 2 : incomplete solution
let unique = "";
// count no. of times it occurs
for (let element of s) {
//compare against rest of characters
for (var i=0; s.length-1; i++){
//if (element != element[i] )
if(unique=s.includes(element)===false)
{return(s.indexOf("unique"))
}else{return(-1);}
}}
